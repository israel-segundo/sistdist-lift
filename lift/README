------------------------------------------
How to Run Client
------------------------------------------

- Clean, compile and generate the jar file:
	$ ./lift-build.sh
	
- Export the LIFT_HOME var pointing to installation directory:
	$ export LIFT_HOME=/home/lift/

- How to run lift-client:
	$ ./lift.sh <params>
	$ ./lift.sh --help

